"""Skill manifest operations — stub."""


def build_manifest():
    """Build a manifest of all skills from registered repos."""
    raise NotImplementedError("manifest.build_manifest is a stub")


def validate_manifest(manifest: dict) -> list[str]:
    """Validate a manifest, returning a list of errors (empty if valid)."""
    raise NotImplementedError("manifest.validate_manifest is a stub")
